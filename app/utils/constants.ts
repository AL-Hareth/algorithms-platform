type Review = {
    title: string;
    description: string;
    href: string;
};

type TFile = {
    title: string;
    description: string;
    href: string;
};

export const reviews: Review[] = [
];

export const files: TFile[] = [
];
